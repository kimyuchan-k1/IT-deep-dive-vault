# 매일 카카오톡으로 받는 백엔드/인프라 딥다이브 — 프로젝트 설계서 v1

작성일: 2026-06-01
작성자: Claude (with 김유찬)

---

## 0. 한 줄 요약

매일 오전 9시, **카카오톡 "나에게 보내기"** 로 **2분코딩 스타일 카드 1장**(핵심 인사이트) + **자세히 보기 버튼**(상세 페이지)을 받는다. 중복 방지를 위한 **로컬 SQLite + 임베딩 DB**를 두고, 모든 콘텐츠는 **Markdown 원본**으로 Git에 보관한다. **GitHub Actions cron**이 이 전체를 자동으로 굴린다.

---

## 1. 영상 4개에서 뽑아낸 "정보 전달 포맷"

조사한 영상 3편의 실제 내용을 분석한 결과(나머지 1편은 동일 URL 중복):

| 영상 | 채널 | 길이 | 핵심 패턴 |
|------|------|------|----------|
| Redis BigKey & HotKey 문제 | 코딩하는기술사 | 짧음 | **문제 정의 → 왜 위험한가 → 대응책** |
| 대규모 예매에서 Kafka 대신 Redis를 쓰는 이유 | 코딩하는기술사 | 짧음 | **현장 사례 → 일반 통념 반박 → 선택 근거** |
| CAP 정리, 잘못 쓰고 있나? | LearnThatStack | 19분 | **오해된 개념 → 원저자 해명 → 현대 패턴(CRDT/Saga/Local-First)** |

### 공통된 "딥다이브 카드" 골격
세 영상 모두 같은 골격을 쓴다 — 이걸 그대로 카카오톡 카드 템플릿으로 만든다.

1. **후킹 한 줄** — "Kafka 안 쓰는 이유?", "Redis는 이렇게 죽는다"
2. **흔한 오해/통념** — 사람들이 보통 어떻게 생각하는지
3. **실제 원리/사실** — 왜 그 통념이 틀렸는지
4. **현장 시나리오 1개** — 대규모 예매, ATM, 디저트 캐시 등 구체적 예시
5. **실무 적용 포인트** — 그래서 내가 뭘 해야 하나
6. **(선택) 더 깊은 토끼굴** — 관련 키워드/논문/RFC 1-2개

### 다루는 주제 카테고리
영상들이 모두 **"백엔드 시니어/아키텍트로 가는 길"**에 있는 주제다:
- 분산 시스템 이론 (CAP, PACELC, CRDT, Saga, Local-First)
- 캐시/큐 (Redis BigKey/HotKey, Sorted Set, Kafka vs Redis)
- DB 내부 동작, 인덱스, 락
- 메시지 큐, 이벤트 드리븐
- 트래픽 폭증/선착순 시스템
- 마이크로서비스 경계와 트랜잭션

→ 우리 주제 카탈로그도 이 8개 카테고리로 구성한다 (4번 섹션 참조).

---

## 2. 전체 아키텍처

```
   ┌─────────────────────────────────────────────────────────────┐
   │                  GitHub Actions (cron: 매일 09:00 KST)       │
   └─────────────────────────────────────────────────────────────┘
                              │
                              ▼
   ┌─────────────────────────────────────────────────────────────┐
   │  ① 주제 후보 생성 (Claude API)                              │
   │     - topics_catalog.md 에서 카테고리/난이도 가중 샘플       │
   │     - "오늘은 분산시스템 카테고리에서 2개 후보 생성해줘"     │
   └─────────────────────────────────────────────────────────────┘
                              │
                              ▼
   ┌─────────────────────────────────────────────────────────────┐
   │  ② 중복 체크 (SQLite + sqlite-vec)                          │
   │     - 과거 전송한 주제 임베딩과 코사인 유사도 > 0.85 → 탈락 │
   │     - 살아남은 후보 1개 선택                                 │
   └─────────────────────────────────────────────────────────────┘
                              │
                              ▼
   ┌─────────────────────────────────────────────────────────────┐
   │  ③ 콘텐츠 생성 (Claude API)                                 │
   │     - 카드용 짧은 버전 (180자 이내, 200자 제한 대비)         │
   │     - 상세 페이지용 긴 버전 (Markdown, 2000~5000자)          │
   └─────────────────────────────────────────────────────────────┘
                              │
                ┌─────────────┴─────────────┐
                ▼                           ▼
   ┌────────────────────────┐   ┌────────────────────────────┐
   │ ④-A 상세 페이지 배포   │   │ ④-B DB 적재                │
   │  posts/2026-06-01.md   │   │  posts 테이블 insert       │
   │  → GitHub Pages        │   │  + 임베딩 저장             │
   │  → 공개 URL 생성       │   └────────────────────────────┘
   └────────────────────────┘
                │
                ▼
   ┌─────────────────────────────────────────────────────────────┐
   │  ⑤ 카카오 API 호출 (/v2/api/talk/memo/default/send)          │
   │     - 피드 템플릿: 제목 + 미리보기 + "자세히 보기" 버튼      │
   │     - 버튼 클릭 → GitHub Pages 상세 글로 이동               │
   │     - refresh_token 으로 access_token 자동 갱신             │
   └─────────────────────────────────────────────────────────────┘
                              │
                              ▼
                  📱 김유찬님 카카오톡 "나와의 채팅"
```

### 왜 이 구조인가
- **카카오 카드는 짧다 (200자)**. 그래서 카드는 "후킹 + 핵심 한 줄"만 담고, 진짜 딥다이브는 외부 페이지로 뺀다.
- **상세 페이지가 GitHub Pages인 이유**: 추가 호스팅 비용 0원, 카카오 도메인 등록 1회면 끝, MD 그대로 정적 페이지로 렌더.
- **GitHub Actions가 cron인 이유**: 컴퓨터가 꺼져 있어도 굴러간다. 로컬 cron은 노트북이 켜져 있어야 함.

---

## 3. DB 트레이드오프 비교표

목적: **중복 방지 + 과거 콘텐츠 검색**.

| 항목 | 로컬 MD 파일 | **SQLite + sqlite-vec** ✅추천 | MySQL/Postgres | Notion DB |
|------|-------------|-------------------------------|----------------|-----------|
| **초기 셋업** | 0분 (그냥 폴더) | 5분 (pip install) | 30분+ (서버/계정/방화벽) | 10분 (DB 페이지 만들기) |
| **운영 비용** | 무료 | 무료 | 호스팅비 월 $5~15 | 무료 (개인) |
| **중복 체크 방식** | 파일명/grep | SQL + 벡터 유사도 | SQL + pgvector | 제목 exact match만 가능(API 한계) |
| **의미 기반 중복 감지** | ❌ (제목 똑같아야 잡힘) | ✅ "Redis 빅키" ≈ "Redis 큰 키 문제" 감지 | ✅ | ❌ |
| **RAG 확장성** | ❌ | ✅ 그대로 RAG로 진화 가능 | ✅ | ⚠️ API 호출 비용/제한 |
| **사람이 직접 보기** | ✅ 최고 (에디터에서 그대로) | ⚠️ DB Browser 필요 | ⚠️ 클라이언트 필요 | ✅ 최고 (웹UI) |
| **백업/이전** | git push 끝 | 파일 1개 복사 끝 | 덤프/리스토어 | export 필요 |
| **모바일 접근성** | ⚠️ (GitHub 앱) | ❌ | ❌ | ✅ 카톡으로 빠르게 열림 |
| **장애 포인트** | 0 | 0 | 네트워크/DB 다운 | Notion 다운/API rate limit |

### 결정: **SQLite + 로컬 MD 파일 하이브리드** (사람 친화 + 기계 친화 둘 다 잡기)

```
F:\IT TEAC DATABASE\IT 정보 전달\
├── posts/                       ← MD 원본 (사람이 직접 읽고 편집)
│   ├── 2026-06-01_redis-bigkey.md
│   ├── 2026-06-02_cap-theorem.md
│   └── ...
├── db/
│   └── delivery.sqlite          ← 메타데이터 + 임베딩 (기계가 읽음)
├── scripts/                     ← 자동화 코드
└── topics_catalog.md            ← 주제 풀
```

- **MD가 진짜 데이터** — Git이 백업/이력관리/롤백 다 해줌
- **SQLite는 인덱스** — 빠른 중복체크/검색용. 깨져도 MD에서 재빌드 가능
- **나중에 Notion 미러링** — 모바일 접근성이 너무 아쉽다 싶으면 Notion으로 일방향 미러(주 1회 sync) 추가

### 나중에 정말 RAG로 발전시키고 싶다면
SQLite 안의 임베딩 컬럼만 더 풍부하게 만들면 됨 — 같은 파일에서 그대로 진화. MySQL/Postgres로 갈 필요 없음. 10만 건 이하라면 SQLite로 충분.

---

## 4. 백엔드/인프라 주제 카탈로그 (초안 — 100개)

`topics_catalog.md` 에 들어갈 시드 주제. Claude가 매일 여기서 가중 샘플링해서 후보 생성.

### A. 분산 시스템 이론 (15)
1. CAP 정리의 진짜 의미와 PACELC
2. CRDT — 동시 편집을 가능하게 하는 자료구조
3. Saga 패턴 vs 2PC
4. Local-First Software 아키텍처
5. Lamport Clock vs Vector Clock
6. Quorum 기반 합의 (R + W > N)
7. Eventual Consistency vs Strong Consistency 비용
8. Read-your-writes / Monotonic Read 일관성 모델
9. 분산 락 — Redlock 논쟁
10. Two Generals Problem이 우리 시스템에 끼치는 영향
11. Paxos를 5분만에 직관적으로 이해하기
12. Raft가 Paxos보다 쉬운 이유
13. Idempotency Key 설계
14. 분산 트랜잭션 — Outbox 패턴
15. CDC (Change Data Capture)와 Debezium

### B. Redis / 캐시 (12)
1. BigKey와 HotKey 문제
2. Redis Sorted Set으로 선착순 큐 만들기
3. Redis 영속화 — RDB vs AOF 비교
4. Cache Stampede와 PER, XFetch 알고리즘
5. Cache-Aside vs Write-Through vs Write-Behind
6. Redis Cluster의 hash slot 16384의 의미
7. Pipelining vs Transaction (MULTI/EXEC)
8. Redis Lua 스크립트로 원자적 연산
9. SCAN을 KEYS 대신 써야 하는 이유
10. Redis Streams vs Kafka 비교
11. TTL과 LRU/LFU eviction 정책
12. Redis 6 ACL과 멀티테넌시

### C. 메시지 큐 / 이벤트 드리븐 (12)
1. Kafka 파티션과 컨슈머 그룹 수학
2. Kafka의 정확히 한 번(Exactly-Once) 보장 진짜 동작
3. Dead Letter Queue 패턴
4. At-least-once vs At-most-once 트레이드오프
5. RabbitMQ vs Kafka 결정 트리
6. Backpressure를 다루는 4가지 패턴
7. Event Sourcing 입문
8. CQRS는 언제 진짜 필요한가
9. Saga Orchestrator vs Choreography
10. Kafka Connect / Schema Registry
11. SQS vs SNS vs EventBridge
12. Pull 모델 vs Push 모델

### D. 데이터베이스 내부 (15)
1. B+Tree 인덱스 내부 구조
2. LSM Tree와 RocksDB 동작 원리
3. MVCC가 락을 어떻게 줄이는가
4. Phantom Read와 격리 수준
5. Hash Join vs Merge Join vs Nested Loop
6. Index Skip Scan과 Covering Index
7. Connection Pool 사이즈를 결정하는 공식
8. WAL과 PITR(Point-in-Time Recovery)
9. PostgreSQL VACUUM과 bloat
10. MySQL InnoDB 버퍼풀 튜닝
11. Read Replica의 replication lag 다루기
12. Sharding 전략 (Range, Hash, Directory)
13. 멱등성을 위한 UPSERT 패턴
14. Materialized View vs 캐시 테이블
15. 어림 카운팅 (HyperLogLog)

### E. 네트워크 / 프로토콜 (10)
1. TCP의 Slow Start와 Congestion Window
2. HTTP/2 vs HTTP/3 (QUIC) 비교
3. WebSocket vs SSE vs Long Polling
4. TLS 1.3 handshake 0-RTT
5. DNS 캐시와 TTL 튜닝
6. gRPC가 REST보다 빠른 진짜 이유
7. CDN 캐시 키 설계
8. Reverse Proxy와 L4/L7 로드밸런서
9. Idempotency를 위한 HTTP 메서드 설계
10. Server-Sent Events 실전 운영

### F. 클라우드 / 인프라 (12)
1. Kubernetes Pod이 죽는 흔한 5가지 이유
2. Liveness vs Readiness vs Startup Probe
3. HPA의 진짜 동작 — 메트릭부터 스케일까지
4. Service Mesh (Istio/Linkerd)가 풀어주는 것
5. Sidecar 패턴 트레이드오프
6. AWS VPC 디자인 — 가용영역 분리
7. Spot Instance를 안전하게 쓰는 법
8. S3 일관성 모델 변천사
9. CloudWatch vs Datadog vs Grafana 스택
10. Helm 차트 vs Kustomize
11. Terraform State 백업과 잠금
12. Blue/Green vs Canary vs Rolling 배포

### G. 관측성 / 안정성 (10)
1. SLI / SLO / SLA의 진짜 차이
2. p50, p95, p99 — 어떤 걸 봐야 하나
3. 분산 트레이싱 (OpenTelemetry)
4. Structured Logging이 grep보다 나은 이유
5. Circuit Breaker 패턴
6. Bulkhead 패턴
7. Retry와 Exponential Backoff + Jitter
8. Chaos Engineering 입문
9. Postmortem 잘 쓰는 법 (blameless)
10. Error Budget로 배포 속도 결정하기

### H. 보안 / 인증 (8)
1. OAuth2 4가지 grant type 차이
2. JWT vs Session 쿠키 — 진짜 트레이드오프
3. mTLS와 zero-trust
4. Rate Limiting 알고리즘 (Token Bucket vs Leaky Bucket)
5. SQL Injection 방어 — Prepared Statement가 부족할 때
6. CSRF와 SameSite 쿠키
7. Secret 관리 (Vault vs SSM Parameter Store)
8. RBAC vs ABAC vs ReBAC

→ **총 94개**. 매일 1개씩 보내면 약 3개월치. 운영하면서 계속 추가.

---

## 5. 카카오톡 카드 템플릿 디자인

카카오 텍스트 템플릿은 200자 제한이라 너무 짧다. **피드 템플릿**을 쓴다.

```json
{
  "object_type": "feed",
  "content": {
    "title": "[Day 12] Redis의 BigKey가 클러스터를 죽이는 이유",
    "description": "1MB 넘는 키 하나가 왜 전체 노드를 마비시킬까? 통념과 실제 동작, 그리고 대응책 3가지. 👉 자세히 보기",
    "image_url": "https://<github-pages>/img/redis-bigkey.png",
    "link": {
      "web_url": "https://<github-pages>/posts/2026-06-12-redis-bigkey/",
      "mobile_web_url": "https://<github-pages>/posts/2026-06-12-redis-bigkey/"
    }
  },
  "social": {
    "view_count": 12
  },
  "buttons": [
    { "title": "딥다이브 읽기", "link": { "mobile_web_url": "..." } },
    { "title": "관련 주제 목록", "link": { "mobile_web_url": ".../tag/redis" } }
  ]
}
```

### 카드 본문 작성 룰 (2분코딩 스타일 재현)
- **title**: 후킹 한 줄 — 의문문 또는 "X가 Y하는 이유" 형식
- **description (최대 4줄, ~120자)**:
  - 1줄: 흔한 오해 한 줄로
  - 2줄: 실제 사실 한 줄로
  - 3줄: 그래서 핵심 포인트
  - 4줄: "👉 자세히 보기"
- **버튼 1**: 상세 페이지
- **버튼 2**: 같은 태그의 과거 글 목록 (DB 재활용)

---

## 6. 실행 환경: GitHub Actions로 정한 이유

| 옵션 | 비용 | 신뢰성 | 토큰 보관 | 컴퓨터 꺼져있어도? |
|------|------|--------|-----------|------------------|
| **GitHub Actions** ✅ | 무료 (월 2000분) | 약간 지연(수분) 있지만 안정 | Secrets 안전 | ✅ |
| 로컬 cron | 무료 | 컴 꺼지면 끝 | 로컬 파일 | ❌ |
| AWS Lambda + EventBridge | 거의 무료 | 매우 안정 | SSM/Secrets Manager | ✅ |
| Cowork 스케줄 | 무료 (앱 실행 중) | 앱 의존 | 세션 의존 | ⚠️ |

→ **GitHub Actions** 선택. 5분 안에 셋업 가능, 토큰을 GitHub Secrets에 안전 보관, 로그/실패 알림 자동.

### Workflow 골격
```yaml
# .github/workflows/daily-it-digest.yml
name: Daily IT Digest
on:
  schedule:
    - cron: '0 0 * * *'   # UTC 00:00 = KST 09:00
  workflow_dispatch:       # 수동 트리거도 가능

jobs:
  send:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: '3.12' }
      - run: pip install -r requirements.txt
      - name: Generate & Send
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          KAKAO_REST_API_KEY: ${{ secrets.KAKAO_REST_API_KEY }}
          KAKAO_REFRESH_TOKEN: ${{ secrets.KAKAO_REFRESH_TOKEN }}
        run: python scripts/run_daily.py
      - name: Commit new post & DB
        run: |
          git add posts/ db/
          git commit -m "feat: $(date +'%Y-%m-%d') post" || echo "no change"
          git push
```

---

## 7. 디렉터리/파일 구조 (최종)

```
F:\IT TEAC DATABASE\IT 정보 전달\
├── .github/
│   └── workflows/daily-it-digest.yml
├── posts/                           ← 사람이 읽는 MD (Git이 백업)
│   └── 2026-06-01_redis-bigkey.md
├── db/
│   └── delivery.sqlite              ← (id, date, title, body_md_path,
│                                       category, tags, embedding BLOB)
├── scripts/
│   ├── run_daily.py                 ← 메인 오케스트레이터
│   ├── generate_topic.py            ← Claude로 주제/콘텐츠 생성
│   ├── dedupe.py                    ← sqlite-vec 코사인 검색
│   ├── kakao_send.py                ← 토큰 갱신 + 메시지 발송
│   └── publish_page.py              ← MD → HTML, GitHub Pages 푸시
├── topics_catalog.md                ← 위 100개 주제 풀
├── requirements.txt
├── README.md
└── 00_프로젝트_설계서_v1.md          ← 이 문서
```

---

## 8. 구현 로드맵

| 단계 | 작업 | 예상 소요 |
|------|------|----------|
| **0** | 카카오 디벨로퍼스 앱 등록 + 도메인 등록 + 첫 access/refresh token 발급 | 30분 |
| **1** | `scripts/kakao_send.py` 짜고 텍스트 1개 카톡 발송 PoC | 1시간 |
| **2** | SQLite 스키마 + sqlite-vec 셋업, MD 1개 insert/임베딩 테스트 | 1시간 |
| **3** | Claude API로 카드용 짧은 버전 + 상세 MD 생성 | 1시간 |
| **4** | 중복체크 → 콘텐츠 생성 → MD 저장 → 발송 end-to-end | 2시간 |
| **5** | GitHub Pages 셋업, MD → 정적 사이트 (Hugo/MkDocs/Astro 중 하나) | 1시간 |
| **6** | GitHub Actions workflow 등록, Secrets 등록, dry-run | 30분 |
| **7** | 첫 7일 운영하면서 카드 문구/길이/주제 난이도 튜닝 | 1주 |

→ **총 ~6시간**이면 v1 가동. 다음 주말에 같이 단계 1부터 코드 짤 수 있음.

---

## 9. 열린 질문 (다음 결정 필요)

1. **상세 페이지 사이트 생성기**: Hugo(빠름) vs MkDocs Material(예쁨) vs Astro(현대적) 중 어느 것?
2. **이미지**: 카드용 썸네일 — 매번 직접 만들지, Claude로 SVG 자동생성할지, 카테고리별 고정 이미지 8장만 돌릴지?
3. **주말 모드**: 주말엔 좀 더 긴 글 (시리즈 형식)로 보낼까, 같은 형식으로 갈까?
4. **태그 시스템**: 카테고리(A~H) + 자유 태그(#redis, #kafka...) 둘 다 갈지?
5. **나에게 보내기 외 추가**: 결국 친구들과 공유하고 싶어지면 → 카카오톡 채널 만들지, RSS도 동시 운영할지?

---

## 10. 참고 자료

- Kakao Developers — [기본 템플릿](https://developers.kakao.com/docs/latest/ko/message-template/default), [메시지 REST API](https://developers.kakao.com/docs/latest/ko/kakaotalk-message/rest-api)
- 영상 — [Redis BigKey/HotKey](https://www.youtube.com/watch?v=gTCgEFnoi6U), [Kafka 대신 Redis로 대기열](https://www.youtube.com/watch?v=IjI4DJvZcAs), [CAP Theorem 재해석](https://www.youtube.com/watch?v=5XOgPeChuE4)
- [sqlite-vec (asg017)](https://github.com/asg017/sqlite-vec) — 벡터 검색용
- [PyKakao](https://github.com/WooilJeong/PyKakao) — 카카오 API 파이썬 래퍼
- GitHub Actions [scheduled workflows 가이드](https://dylanbritz.dev/writing/scheduled-cron-jobs-github/)
