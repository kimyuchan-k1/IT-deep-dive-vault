# 매일 카카오톡으로 받는 백엔드/인프라 딥다이브 — 설계서 v2

작성일: 2026-06-01
작성자: Claude (with 김유찬)
이전 버전: [v1](./00_프로젝트_설계서_v1.md)

---

## 0. 한 줄 정체성

> **점진적으로 자라는 개인용 RAG (Personal Memory-Augmented Generation System) — 검색은 임베딩 + 그래프, 생성은 Claude, 출력은 카카오톡 카드.**

이 한 줄에서 모든 아키텍처 결정이 파생된다.

---

## 1. v1에서 무엇이 바뀌었나

| 항목 | v1 | **v2** | 이유 |
|------|----|--------|------|
| 스케줄러 | GitHub Actions cron | **Cowork scheduled task** | 어차피 두뇌는 Claude. GHA로 가도 그 안에서 Claude API를 호출했어야 함. 한 겹 제거. |
| 데이터 위치 | 로컬 폴더만 | **로컬 vault(작업) + GitHub repo(백업/호스팅)** | 누적 자산이라 클라우드 이중화 필수. 이식성·이력관리. |
| 데이터 형식 | MD + SQLite | **Obsidian Vault (MD + `[[wikilinks]]` + frontmatter)** | 위키링크 자체가 그래프 엣지. 별도 그래프 DB 없이 그래프 데이터 확보. |
| 그래프 도입 시점 | v3(3개월 후) | **v1부터** | 위키링크 방식이라 도입 비용 거의 0. 글 5개일 때부터 관계 누적 시작. |
| 시스템 정체성 | "자동 발송기" | **"자라는 personal RAG"** | 생성/검색/저장 역할이 명확해짐. |

---

## 2. 정체성: 왜 "personal RAG"라고 부르는가

| RAG 구성요소 | 우리 시스템 |
|---|---|
| **지식 저장소** | Obsidian Vault (MD + 위키링크 + frontmatter) |
| **검색 인덱스** | `embeddings.sqlite` (의미 검색) + `graph.json` (관계 검색) |
| **검색 트리거** | 매일 09:00 KST Cowork 스케줄 (사용자 질문 대신 시간) |
| **생성기(G)** | Claude (Cowork 안에서 실행) |
| **출력** | 카카오톡 피드 카드 + GitHub Pages 딥다이브 글 |

**일반 RAG와의 결정적 차이**: 외부 지식이 아니라 **본인의 누적 메모리**를 검색한다. → 글이 쌓일수록 "내가 이미 다룬 것을 피하고 미답 영역으로 가도록" 시스템이 스스로 균형을 잡는다.

**보너스**: 같은 인프라가 그대로 **사용자 질의용 RAG로 확장 가능**. ("내가 Redis에 대해 정리한 거 다 보여줘")

---

## 3. 매일 흐름

```
┌─────────────────────────────────────────────────────────────────┐
│  Cowork scheduled task (매일 09:00 KST)                          │
│   → "오늘의 딥다이브 글 작성하고 카카오톡으로 보내줘" 프롬프트   │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  ① 로컬 vault 직접 읽기                                          │
│     F:\IT TEAC DATABASE\IT 정보 전달\posts\*.md                  │
│     - frontmatter 메타 모음                                       │
│     - [[wikilinks]] 그래프 인덱스 로드                            │
│     - 임베딩 SQLite 로드                                          │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  ② 주제 후보 생성 + 중복/관련성 판단 (Claude)                    │
│     - topics_catalog.md에서 후보 3개 샘플                         │
│     - 각 후보별: 임베딩 유사도 > 0.85 → 탈락                      │
│     - 각 후보별: 그래프 인접 노드(2-hop) 검사 → 빈 영역 우선      │
│     - 1개 선정                                                    │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  ③ 콘텐츠 생성 (Claude)                                          │
│     - 짧은 카드 텍스트 (~120자)                                   │
│     - 딥다이브 MD (2-5천자, [[wikilinks]] 포함)                   │
│     - frontmatter: title, date, category, tags, related           │
└─────────────────────────────────────────────────────────────────┘
                              │
                ┌─────────────┴─────────────┐
                ▼                           ▼
┌────────────────────────────┐   ┌────────────────────────────┐
│  ④-A 로컬 vault 저장        │   │  ④-B 임베딩 인덱스 갱신    │
│  posts/2026-06-XX.md       │   │  SQLite에 INSERT           │
└────────────────────────────┘   └────────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────────────────────────────┐
│  ⑤ git add/commit/push (백업 + Pages 빌드 트리거)                │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  ⑥ GitHub Pages 자동 빌드 (~수 분)                              │
│     - MD → 정적 HTML                                             │
│     - 공개 URL 생성                                              │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  ⑦ 카카오 API 호출 (/v2/api/talk/memo/default/send)              │
│     - 피드 템플릿 (제목 + 미리보기 + 버튼 2개)                   │
│     - 버튼 → GitHub Pages 딥다이브                              │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
                  📱 김유찬님 카카오톡 "나와의 채팅"
```

---

## 4. 디렉터리 / 파일 구조

```
F:\IT TEAC DATABASE\IT 정보 전달\        ← 로컬 작업공간 (= GitHub repo와 동기화)
│
├── posts/                                 ← Obsidian Vault. 진짜 데이터.
│   ├── 2026-06-01-redis-bigkey.md
│   ├── 2026-06-02-cap-theorem.md
│   └── ...
│
├── attachments/                           ← 이미지/도식
│   └── redis-bigkey-cluster.png
│
├── .obsidian/                             ← Obsidian 앱 설정 (그래프 뷰 자동)
│   └── workspace.json
│
├── index/                                 ← 파생 인덱스 (vault에서 재빌드 가능)
│   ├── embeddings.sqlite                  ← 임베딩 캐시 (의미 검색)
│   └── graph.json                         ← 위키링크 그래프 인접 행렬
│
├── site/                                  ← GitHub Pages 빌드 결과
│   └── (Hugo/MkDocs 출력)
│
├── scripts/                               ← 자동화 (Cowork이 호출)
│   ├── generate_post.py                   ← 주제 선정 + 글 생성
│   ├── dedupe_check.py                    ← 임베딩 + 그래프 중복 체크
│   ├── update_index.py                    ← 임베딩 SQLite/graph.json 갱신
│   ├── publish_site.py                    ← MD → 정적 사이트
│   └── kakao_send.py                      ← 토큰 갱신 + 카드 발송
│
├── topics_catalog.md                      ← 시드 주제 풀 (94개, v1과 동일)
├── requirements.txt
├── README.md
├── 00_프로젝트_설계서_v1.md
└── 01_프로젝트_설계서_v2.md                ← 이 문서
```

### 역할 분리 (정체성 → 컴포넌트)

| RAG 역할 | 컴포넌트 | 비고 |
|---|---|---|
| Ground truth | `posts/*.md` | 사람이 손으로도 편집 가능 |
| Retrieval (의미) | `index/embeddings.sqlite` | `posts`에서 재빌드 가능 → 깨져도 복구 OK |
| Retrieval (관계) | `index/graph.json` | `posts`의 `[[wikilinks]]`에서 빌드 |
| Generation | Claude (Cowork) | 매일 호출 |
| 사람용 인터페이스 | Obsidian 데스크탑/모바일 앱 | 그래프 뷰 자동 |
| 외부용 인터페이스 | GitHub Pages | 카카오 카드 링크 대상 |
| 백업 / 이력 / 이식 | GitHub repo | private 또는 public |

---

## 5. Obsidian Vault MD 포맷 (표준)

매 글의 형식. Claude가 매일 이 골격대로 생성.

```markdown
---
title: Redis BigKey가 클러스터를 죽이는 이유
date: 2026-06-01
category: redis
tags: [cache, performance, distributed]
related: ["[[redis-hotkey]]", "[[cache-stampede]]", "[[redis-cluster-slot]]"]
difficulty: 3   # 1~5
source_inspiration: youtube/gTCgEFnoi6U   # 영감 출처(선택)
---

# Redis BigKey가 클러스터를 죽이는 이유

## 흔한 오해
사람들은 [[redis-hotkey]]만 위험하다고 생각한다. 하지만 BigKey는 한 노드가 아니라
[[redis-cluster-slot]] 설계 때문에 클러스터 전체를 마비시킬 수 있다.

## 실제 원리
...

## 현장 시나리오
...

## 실무 적용 포인트
1. ...
2. ...

## 더 깊은 토끼굴
- [Redis 공식: Big keys](https://redis.io/...)
- [[lazy-freeing]] — UNLINK 명령의 진짜 이유
```

### 핵심 규칙
- **`[[wikilink]]`** = 다른 글로의 그래프 엣지. 글 슬러그(파일명에서 날짜 뗀 부분)와 일치
- **`related:` frontmatter** = 본문에 등장 안 해도 강제로 그래프 엣지 만들 때
- **`category`** = topics_catalog.md의 8개 카테고리(A~H) 중 하나
- **`tags`** = 자유 태그
- **`difficulty`** = 카드에서 "🔥🔥🔥" 같은 시각 표시용

---

## 6. 카카오 피드 카드 (변경 없음)

```json
{
  "object_type": "feed",
  "content": {
    "title": "[Day 1] Redis의 BigKey가 클러스터를 죽이는 이유 🔥🔥🔥",
    "description": "BigKey는 한 노드 문제로 보이지만, 실은 클러스터 전체를 마비시킨다. 왜? 👉",
    "image_url": "https://<pages>/img/redis-bigkey.png",
    "link": { "mobile_web_url": "https://<pages>/posts/redis-bigkey/" }
  },
  "buttons": [
    { "title": "딥다이브 읽기", "link": { "mobile_web_url": "https://<pages>/posts/redis-bigkey/" } },
    { "title": "관련 글 (#redis)", "link": { "mobile_web_url": "https://<pages>/tags/redis/" } }
  ]
}
```

설계서 v1의 200자 제한, 도메인 등록 규칙은 동일하게 유효.

---

## 7. 그래프가 매일 어떻게 쓰이나 (구체적으로)

### 글 생성할 때
1. 후보 주제 임베딩과 기존 글 임베딩 유사도 → **0.85 초과 = 중복으로 탈락**
2. 후보 주제와 그래프상 1-hop 거리 노드들의 카테고리 분포 검사
   - 같은 카테고리만 잔뜩 → 다른 카테고리 후보 우선
   - 고립 영역 노드 인접 → **우선 채택** (지식 공백 메우기)
3. 본문 작성 중 Claude가 기존 글 frontmatter 슬러그 목록 보고 자연스러운 `[[wikilink]]` 삽입

### 카드 보낼 때
- 두 번째 버튼이 "관련 글" — 그래프상 직접 연결된 글 목록 페이지로 이동

### 주간 회고용 (선택)
- 그래프 시각화로 "이번 주 다룬 영역" 한눈에 확인
- 가장자리 고립 노드 = "한 번만 다뤘는데 후속 글 없는 주제" → 다음 주 우선순위

---

## 8. 인덱스 빌드 전략

`posts/`가 진실의 원천이라, 인덱스는 언제든 재생성 가능.

| 인덱스 | 빌드 시점 | 비용 |
|---|---|---|
| `embeddings.sqlite` | 새 글 작성 직후 incremental insert | 글당 임베딩 1회 |
| `graph.json` | 새 글 작성 직후 incremental update | O(1) |
| 전체 재빌드 | 인덱스 손상 시 또는 주 1회 안전망 | 글 N개일 때 N회 임베딩 |

---

## 9. 운영 가드레일

| 위험 | 대응 |
|---|---|
| Cowork 스케줄 누락 | 첫 7일 매일 카톡 도착 여부 수동 확인. 누락시 즉시 manual trigger. |
| Cowork 종료/변경 (research preview) | 모든 스크립트는 Cowork 없이 CLI로도 실행 가능하게 작성. GHA 이관 옵션 항시 살아있게. |
| Kakao access_token 만료 | refresh_token으로 자동 재발급. 실패시 다음날 알림. |
| Kakao 도메인 등록 누락 | 카드 버튼 작동 안 함. 셋업 1회로 끝나는 항목. |
| GitHub Pages 빌드 실패 | Pages는 비동기라 발송 시 미완성 가능. → 카드 발송 전 URL HEAD 체크 + 1회 재시도. |
| 중복 임계값 오인식 | 임계값(0.85) 조정 가능하게 환경변수화. 첫 2주 로그 보고 조정. |
| LLM 환각 (잘못된 기술 정보) | 매 글 끝에 1차 출처(공식 문서 URL) 1개 이상 강제. 사람이 주 1회 검수. |

---

## 10. 구현 로드맵 (수정판)

| 단계 | 작업 | 예상 소요 | 사용자 작업 | Claude 작업 |
|------|------|----------|-------------|-------------|
| **0** | Vault 디렉터리 + 첫 글 1편 수동 작성 (포맷 검증) | 즉시 | ☐ 검토 | ✅ 이 대화에서 같이 |
| **1** | GitHub repo 생성 + 첫 푸시 | 10분 | ☐ repo 만들기 | 가이드 제공 |
| **2** | 카카오 디벨로퍼스 앱 등록 + 토큰 발급 | 30분 | ☐ 앱 등록 | 단계별 가이드 |
| **3** | `kakao_send.py` 작성 + 텍스트 1건 발송 PoC | 1시간 | ☐ 토큰 secret 등록 | ✅ 코드 작성 |
| **4** | 임베딩 SQLite + 그래프 빌더 작성 | 2시간 | - | ✅ 코드 작성 |
| **5** | 글 생성 파이프라인 (`generate_post.py`) | 2시간 | - | ✅ 코드 작성 |
| **6** | GitHub Pages 셋업 (MkDocs Material 추천) | 1시간 | ☐ Pages 설정 | ✅ 설정 |
| **7** | Cowork 스케줄 등록 (manual_trigger로 1회 테스트 → 매일 자동) | 30분 | ☐ 동의 | ✅ 스케줄 등록 |
| **8** | 첫 7일 운영 + 카드 문구/난이도/중복 임계값 튜닝 | 1주 | ☐ 매일 피드백 | ✅ 튜닝 |

**v1 목표 (1주차 종료 시점)**: 매일 09:00 자동 카톡 1건, 누적 7편의 vault, Obsidian 그래프 뷰에서 5개 이상의 노드 연결.

---

## 11. 다음 결정 (마이너)

이전 v1에서 열려있던 항목들. 빠르게 결정하면 좋음:

1. **GitHub Pages 사이트 생성기**:
   - **MkDocs Material** ✅ (추천) — 한국어 검색 강함, 태그/카테고리/검색 기본 제공, 셋업 30분
   - Hugo — 빠름. 셋업 시간 비슷
   - Astro — 현대적. 학습 곡선

2. **GitHub repo public/private**: public이면 GitHub Pages 무료, 학습 흔적 공개로 부수 효과. private이면 내 메모로만.

3. **카드 썸네일**: 카테고리별 고정 이미지 8장 추천 (셋업 1회로 끝).

4. **주말 모드**: v1은 평일/주말 동일 포맷. 운영 후 보고 분기.

---

## 12. 참고자료

- v1 설계서 (변경 이력)
- Kakao Developers: [기본 템플릿](https://developers.kakao.com/docs/latest/ko/message-template/default), [메시지 REST API](https://developers.kakao.com/docs/latest/ko/kakaotalk-message/rest-api)
- 영감 영상: [Redis BigKey/HotKey](https://www.youtube.com/watch?v=gTCgEFnoi6U), [Kafka 대신 Redis](https://www.youtube.com/watch?v=IjI4DJvZcAs), [CAP Theorem](https://www.youtube.com/watch?v=5XOgPeChuE4)
- 도구: [sqlite-vec](https://github.com/asg017/sqlite-vec), [PyKakao](https://github.com/WooilJeong/PyKakao), [Obsidian](https://obsidian.md), [MkDocs Material](https://squidfunk.github.io/mkdocs-material/)
- 개념: GraphRAG (Microsoft, 2024) — 그래프+RAG가 flat RAG 능가하는 이유
