# 다음 단계 체크리스트

설계서 v2 기반으로 김유찬님이 진행할 작업 + Claude가 도와줄 작업 분리.

---

## 즉시 할 일 (사용자 작업)

### ☐ 1. 첫 글 검토 (5분)
- 파일: [`posts/2026-06-01-redis-bigkey.md`](./posts/2026-06-01-redis-bigkey.md)
- 확인 포인트:
  - **카드용 짧은 텍스트** (frontmatter `short_text`) — 카톡에서 받아보면 매력적인가?
  - **본문 깊이** — 너무 길거나/짧지 않은가? 2분코딩 스타일과 비슷한가?
  - **[[wikilinks]] 사용** — 자연스러운가?
  - **카테고리/난이도/태그** — 잘 분류됐는가?
- 피드백 주면 → 포맷 가이드라인 확정 후 매일 이 형식으로 자동 생성됨

### ☐ 2. GitHub repo 생성 (10분)
- repo 이름 후보: `it-deep-dive-vault` / `daily-it-digest` / `kbo-vault` 등
- private vs public:
  - **private** — 내 메모. GitHub Pages는 유료 플랜 필요
  - **public** ✅ 추천 — 학습 흔적 공개. Pages 무료. 부수 효과로 본인 기술 블로그 됨
- 생성 후 로컬 폴더 git init → remote add → push
- 명령어 가이드는 셋업 단계 진입 시 Claude가 제공

### ☐ 3. 카카오 디벨로퍼스 앱 등록 (30분)
필수 단계:
1. https://developers.kakao.com 가입/로그인
2. "내 애플리케이션" → "애플리케이션 추가하기" → 앱 이름/회사명 입력
3. **앱 키** 발급 받기 (REST API 키)
4. **카카오 로그인** 활성화 → 동의항목에서 "카카오톡 메시지 전송" 권한 추가
5. **플랫폼 등록**:
   - Web → 도메인에 `https://<github-username>.github.io` 등록
6. **Redirect URI 등록** (토큰 발급 1회용): `https://localhost:8080` 같은 임시 URI도 OK
7. 첫 access_token + refresh_token 발급 (가이드 별도 제공)

→ **이 단계 진입 시 Claude가 단계별 스크린샷 가이드 + 토큰 발급 코드 제공**.

---

## Claude가 다음에 할 일 (사용자가 위 1~3 완료하면)

### 단계 3 — Kakao 토큰 발급 헬퍼
- `scripts/get_initial_token.py` — 첫 토큰 발급 1회용
- `scripts/kakao_send.py` — refresh_token으로 access_token 갱신 + 텍스트 발송
- PoC: 텍스트 "안녕!" 1건 카카오톡 발송 성공

### 단계 4 — 임베딩 + 그래프 인덱스
- `scripts/build_index.py` — `posts/*.md` 전체 스캔
- 각 글에 대해:
  - 임베딩 생성 (Claude API or OpenAI or local model)
  - `embeddings.sqlite` insert
  - frontmatter `related` + 본문 `[[wikilinks]]` 추출 → `graph.json`
- `posts/2026-06-01-redis-bigkey.md`로 첫 인덱스 빌드 테스트

### 단계 5 — 글 생성 파이프라인
- `scripts/generate_post.py`
- 입력: 오늘 날짜, topics_catalog.md
- 처리:
  1. 후보 3개 샘플 (카테고리 균형)
  2. 각 후보 임베딩 vs 기존 글 임베딩 → 중복 탈락
  3. 그래프 인접 노드 분석 → 빈 영역 우선
  4. 1개 선정 → Claude로 본문 작성 → MD 저장
  5. 인덱스 갱신
  6. git commit/push

### 단계 6 — GitHub Pages
- MkDocs Material 셋업
- `mkdocs.yml` + 네비게이션 + 검색 + 태그 페이지
- GitHub Actions 1개만 추가: push마다 mkdocs build → Pages 배포
- 카카오 카드 버튼 URL 검증

### 단계 7 — Cowork 스케줄 등록
- `mcp__scheduled-tasks__create_scheduled_task`로 매일 09:00 KST 등록
- 프롬프트 템플릿:
  ```
  posts/ 에서 오늘의 새 글을 생성해줘.
  1. topics_catalog.md에서 후보 3개 선정
  2. scripts/dedupe_check.py로 중복 검사
  3. scripts/generate_post.py로 본문 작성
  4. scripts/build_index.py로 인덱스 갱신
  5. git push로 백업
  6. scripts/kakao_send.py로 피드 카드 발송
  발송 후 결과 요약 한 줄.
  ```
- **manual_trigger로 1회 테스트** → 다음날 자동 실행 확인 → 본격 가동

### 단계 8 — 운영 + 튜닝 (1주차)
- 매일 카드 도착 확인
- 글 품질 피드백 → 프롬프트 튜닝
- 중복 임계값 조정 (0.85 → 0.80 등)
- 난이도 균형 조정

---

## 첫 결정 (간단)

이전 v2 설계서 11번에서 열린 항목:

| 결정 | 옵션 | 추천 |
|---|---|---|
| 사이트 생성기 | MkDocs Material / Hugo / Astro | **MkDocs Material** |
| GitHub repo 공개 여부 | public / private | **public** (Pages 무료 + 학습 공개) |
| 카드 썸네일 | 카테고리별 고정 8장 / 글마다 생성 | **카테고리 고정 8장** (셋업 1회로 끝) |
| 주말 모드 | 평일/주말 동일 / 주말 더 깊게 | **일단 동일**, 7일 운영 후 재논의 |

이 4개만 OK 주시면 단계 진행 가능.

---

## 한눈 요약

```
지금 ───────────────────────────────────────────► 1주 후
첫 글 검토          GitHub repo + 카카오 앱       Cowork 스케줄 가동
     ↓                       ↓                          ↓
포맷 OK             토큰 발급 완료            매일 09:00 자동 카톡
                            ↓                          ↓
                   스크립트 전부 작성        Obsidian 그래프 뷰
                                                  본격 활용
```
