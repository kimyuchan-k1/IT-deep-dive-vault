# 모범 글 예시

매일 새 글을 쓸 때 **반드시 이 문서를 읽고** 톤/구조/길이를 모방한다.

좋은 글이 나올 때마다 여기 박제. 첫 글부터.

---

## 예시 1 — Day 1: Redis BigKey

**파일**: [`posts/2026-06-01-redis-bigkey.md`](./posts/2026-06-01-redis-bigkey.md)

### 왜 이게 모범인가

✅ **후킹 제목** — "Redis BigKey가 클러스터 전체를 마비시키는 이유"  
   → "전체"라는 강한 단어, "마비"라는 결과 명시, "이유"로 호기심 유발

✅ **흔한 오해 명확**:
> "BigKey는 그 키가 있는 한 노드에서만 느려지는 문제 아닌가?"
   → 따옴표로 묶어 실제 사람의 말처럼 제시
   → "그리고 [[redis-hotkey]]가 더 위험하다고 말한다" — 통념의 다음 단계까지 정확히 짚음

✅ **실제 원리 — 두 핵심 메커니즘 설명**:
- "Redis는 싱글 스레드다" 섹션: 1MB → 수십~수백 ms, 10MB → 초 단위 (구체 수치)
- "그래서 클러스터 전체로 어떻게 번지나" 섹션: 4단계 인과 사슬 (커넥션 풀 → 헬스체크 → 복제 지연 → AOF/RDB)

✅ **현장 시나리오 인과 명확**:
> 5MB hash → DEL 200ms 블로킹 → cluster-node-timeout 150ms 초과 → 잘못된 페일오버 → 1분간 장애 → 원인 한 줄: "DEL one key."

   → 숫자 (5MB, 200ms, 150ms, 1분), 명령 (`DEL guild:chat:12345`), 결과 (전 게임 서비스 응답 실패) 다 있음

✅ **실무 적용 6개 — 다 구체적**:
1. `redis-cli --bigkeys` (명령)
2. UNLINK ([[lazy-freeing]] 링크)
3. 5MB → 50KB × 100 샤딩 (구체 수치)
4. HSCAN 사용, 100개씩 (구체 수치)
5. cluster-node-timeout 150ms 너무 빡빡, 500ms~1s 권장 (구체 수치 + 트레이드오프)
6. hash-max-ziplist-entries 임계 (구체 플래그)

✅ **출처**: Redis 공식 latency 가이드 URL + 영감 받은 YouTube URL

✅ **[[wikilinks]] 5개**: redis-hotkey, redis-cluster-slot, lazy-freeing, cache-stampede

✅ **200자 카드** (`short_text`):
```
🔥 [Day 1] Redis 명령 1개로 클러스터 6노드 마비
오해: BigKey = 한 노드만 느려진다
실제: 블로킹→풀 고갈→헬스체크 실패→페일오버 캐스케이드
"DEL guild:chat:12345" 한 줄이 원인이었다.
📖 {URL}
```
   → 이모지 🔥, 임팩트 한 줄, 오해/실제 대조, 인용된 사례, URL

### 분량
- 본문 약 2,300자 (목표 2,000~5,000자 안)
- 섹션 5개 (흔한 오해, 실제 원리, 현장 시나리오, 실무 적용, 더 깊은 토끼굴)
- 코드 블록 0개 (이 주제는 명령어 인라인으로 충분)

### 톤 분석
- "둘 다 맞는데, 절반만 맞다." — 통념을 부정하지 않으면서 정확한 결을 짚음
- "여기가 핵심이다:" — 같이 파고드는 톤
- "요약:" 마지막 한 줄 — 인과 사슬 압축 (사족 아님)
- 가르치는 투 없음. 같이 분석하는 톤.

### 다시 쓸 거였으면 더 좋게 할 부분
- 도식이 있으면 더 좋음 (싱글 스레드 블로킹 → 풀 고갈 → 캐스케이드 그림)
- "Redis 6 이후 I/O 멀티스레딩" 부분에 공식 문서 링크 추가 가능

---

## 향후 예시 후보 (좋은 글 나오면 추가)

- [ ] 분산 시스템 카테고리 모범 (CAP/PACELC급)
- [ ] DB 내부 카테고리 모범 (B+Tree/MVCC급)
- [ ] 네트워크 카테고리 모범 (HTTP3/TCP급)
- [ ] 보안 카테고리 모범 (OAuth/JWT급)

→ 카테고리별 1개씩 박제하면 매일 톤 일관성 보장.

---

## 사용 규칙

매일 글 쓰기 전에:
1. 이 문서를 읽는다
2. 오늘 쓸 글과 같은 카테고리의 예시가 있으면 그것을, 없으면 Day 1 Redis BigKey를 모범으로 삼는다
3. 그 글의 톤/길이/구조를 흉내낸다

이게 personal RAG의 "in-context learning" 부분.
